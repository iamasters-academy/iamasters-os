# working-memory.md — scratchpad de trabajo
<!-- Tope: ~2.500 caracteres. Se carga al inicio de cada sesión. -->
<!-- REGLAS: solo hechos accionables; no incluir código ni outputs largos; dedup antes de añadir. -->

## Hilos activos
<!-- Qué proyectos/tareas están en progreso ahora mismo -->
- **App video / FVI** — rama `feat/fase-1` (`projects/app video`, repo `Capitalmaxequity/agencia-video-analisis`, ~13 commits pusheados hasta `6aa5d59`). **VIVE EN EL VPS** `~/app-video` (Docker `fvi`, acceso `ssh dev@144.91.91.233` por clave). Análisis de broadcast RESUELTO por **segmentos de plano abierto** (`scripts/pick_live_segments`+`run_segments --merge`, cobertura 39-63%, informe único de partido); balón **WASB** integrado (0,7%→71%). Detalle completo en daily-summary 05/07 Sesión 3. **ÚNICO PENDIENTE GRANDE = Frente A (entrenar 4 modelos propios en GPU alquilada)**: Vía B eventos T-DEED (sube shot/recovery — el gate no pasa por algoritmo, confirmado), keypoints calibración (MMPose Apache), reentreno balón WASB (licencia dataset pspagnolo sin verificar), OCR dorsales (PARSeq). **Los slots del pipeline YA están montados** (ONNX enchufables, no-op sin modelo); guía con scripts/datasets/licencias en `docs/GUIA_MODELOS_PROPIOS_GPU.md`. Cuando Olaf alquile GPU → preparar los 3 scripts (entrenar/export-ONNX/validar) por modelo. Modelos de valor xG/VAEP comerciales YA hechos (Wyscout CC BY, atribución obligatoria en entregables). GTM del servicio en `docs/GTM_SCOUTING_SERVICE.md`. Gotchas: `--entrypoint python` para scripts (ENTRYPOINT=`python -m`); comandos largos en tmux; NUNCA `git add -A` (colaría empresa-scouting).
- **Polymaster research copilot** (`projects/polymaster/`, `Capitalmaxequity/polymaster@master`): **(02/07) DASHBOARD PÚBLICO + REAL, sin pendientes.** Desplegado en VPS `http://144.91.91.233:8100` (Basic Auth, credenciales solo en `.env` VPS), 2 buscadores reales (mercados Polymarket en vivo + full-text research), pipeline pasó de mock a real (Ollama qwen3.5:9b, `--allow-restricted` porque Polymarket marca 100% restricted por geo-IP alemana, no bloquea lectura), **cron cada 6h instalado y verificado** (`scripts/run_forecast.sh`, flock+nice). Commits `2c1141b`→`741b7e7`. "UNCALIBRATED" persistirá hasta que resuelvan mercados reales — esperado. Nada que hacer, solo mirarlo de vez en cuando.
- **Polymarket Copytrading** (`Capitalmaxequity/polymaster-copytrader`): 8 líderes en SHADOW en el VPS. **(01/07) Revisión dashboard + fix bot congelado + móvil** en rama `fix/dashboard-and-frozen-bot` (commit `2996034`, **pushed a origin**, suite 442 verde). **Diagnóstico**: el runner NUNCA resolvía posiciones shadow (`resolve_open_shadow_trades` solo lo llamaba el backtest) → 674 abiertas/$3.370 exposición → gate "max open exposure" bloqueaba TODO; y el HWM (contaminado por backtests en la BD de prod) fijaba drawdown 75% → circuit breaker 6%/24h en halt perpetuo. "No salen los 8 líderes" = tab equivocado (Trader Profiles muestra scoring forense, no los copiados; los 8 SÍ salen en Operational Console). **Código hecho**: runner cablea resolución cada N ciclos; `reset_drawdown_state` + CLI `python -m app.risk.reset_drawdown`; halt configurable `RISK_HALT_DRAWDOWN`/`_DURATION_SECS` (default 15%/1h); TraderProfiles relabel + badge ✓copiado (join /api/wallets); móvil (tabs scroll, anti-overflow, banner 44vh). **DEPLOY COMPLETO Y VERIFICADO (01/07)**: ① descongelado (658/674 resueltas → exposición $3.370→$80, HWM $3.622→$970.83, halt OFF; backup `bot.sqlite.bak-20260701-083417`); ② código nuevo desplegado vía tarball+scp+`docker compose build && up --force-recreate` (imagen rebuild con leaderboard.py preservado; settings activos: RESOLVE_EVERY_N_CYCLES=20, RISK_HALT_DRAWDOWN=0.15, DURATION=3600; frontend `TraderProfiles-sAgGtuya.js` con "Scoring forense" en vivo); ③ auth: el `DASHBOARD_API_TOKEN` del `.env` estaba **vacío** → fijado a `72Y1Bdq_EAahiIrcJe28JYHGI-66uB6fU-8xIAmmEr8` (longitud 43); **verificado 401 sin token**. Estado final: running:true, halted:false, drawdown:0, exposure $80, 8 wallets OK. **Gotcha**: probar auth con POST a `/api/kill-switch` LO ACTIVÓ (auth aún off entonces) → reseteado con token + bot re-arrancado. **PR #2 MERGEADO a main** (squash `8e35ed2`, con `--admin` porque la CI lint está roja: deuda pre-existente de ruff en TODO el repo — main llevaba 3 commits en rojo; mis ficheros quedan limpios, arreglé `reset_drawdown.py` UP031). **PENDIENTE**: ④ Olaf `ssh polymaster-vps 'sudo ufw deny 8010/tcp && sudo ufw reload'` (necesita sudo); limpiar deuda de lint ruff repo-wide (CI verde); repaso PnL por líder en 4-7 días. Autorización estable: reglas `Bash(ssh polymaster-vps:*)`/`Bash(scp:*)` en settings.local.json. VPS checkout `/home/dev/polymaster-copytrader`. Ver [[copytrading-unificacion]].
- **Mente Táctica AI** (`Capitalmaxequity/LLM`, rama `feat/remediacion-auditoria` → **PR #1 abierto**): remediación de la auditoría **Fases 7-9 cerradas** (todos los P0+P1 + P2-seguro), **`tsc` 0 / jest 72/72 / coverage ratchet en CI**. **(30/06) Decisión Verify-first** (seis-sombreros): validar en dispositivo ANTES de seguir/lanzar → correr `mobile/docs/SMOKE-TEST-fase7.md` (6 flujos + perf LLM/voz + compra sandbox RevenueCat); sus fallos = backlog real; luego mergear PR #1. Plan B si perf on-device inviable → harden-thin/freeze. **🔴 OLAF: rotar claves del repo público** (RevenueCat, OAuth Google/Apple, n8n webhook, PAT Supabase `sbp_4849…`); avisar a clones: **re-clonar** (historia reescrita por la purga). Backlog higiene (no bloqueante): 221 unused→reactivar `noUnusedLocals`, subir ratchet, lint/audit bloqueantes, dedup 4 capas Sync, P1 fuera de F7. Detalle `mobile/tmp/audit/findings_final.md` · [[mente-tactica-auditoria-2026-06-26]]. **(01/07) REVIEW-IA de la superficie IA**: revisión adversarial (9 subsistemas, escépticos) → **73 hallazgos** (14 P0·30 P1·29 P2), informe `mobile/docs/REVIEW-IA-2026-07-01.md`. Fixes seguros en rama **`review/ia-surface`** (desde feat/remediacion-auditoria), tsc verde por lote: L1 honestidad (mock cards/insight/hardware falso→null), L2 RAG/memoria (fallback LiteEmbedder, no perder memorias/crash), L3 guardrails (inyección/escape RAG/veto médico fail-safe/sanitize), L4 integridad modelos (checksum+status HTTP descargas), L5 routing (formación fallback/ruta válida/effectiveRole), L6 voz, L7 dead-code. **13 commits, ~40 hallazgos, tsc verde + jest 72/72**: honestidad, RAG/memoria (fallback LiteEmbedder), guardrails (inyección/escape RAG/veto fail-safe/sanitize/PII-a-cloud), integridad modelos (checksum+status+md5-lazy), routing/parsing (R01/F1·F4-PARSE), voz (ordinales/gaps/barge-in), INF-02(overflow two-pass)/INF-04(retry counter), ERM-12, dead-code (useVisionScout/voiceStore/SmartWhisperManager+useVoiceInput). **PENDIENTE**: (a) RISK — **F2·PARSE hecho+test (74/74)**, **V02 hecho** (recovery breaker); quedan solo-device V03/F2·AGT(fix)/INF-03/07-térmico/PM-09 (smoke test); (b) decisiones — **HECHAS**: ERM-03 (ingestor honesto stored_not_indexed), R03 (no doble-ejecución en acciones), ERM-06 (rowid unificado al esquema vivo), ERM-07 (extracción LLM documentada como dormida); (c) roster **HECHO** — GS-04/07 anonimiza nombres del playerStore antes de cloud; (d) dead-code **HECHO** — GS-06 (valida artifact enum), GS-08 ya cubierto (useMisterAI ya sanitiza+guard); (e) P2 **HECHO F7-PARSE** (schema en fallback); **skip triados** (churn/regresión/device): R06 catch defensivo, F6·AGT regex rompería nombres equipo, INF-01 default ya cubre, INF-06 ya logea, INF-09 aproximación no-crash, GS-10/R11/R12 dedup/fuzzy con riesgo, F8/F9-PARSE riesgo, V08 marginal, V10→device. **Falta**: mergear rama a PR#1 tras smoke test en device. **(01/07) AUDITORÍA CONOCIMIENTO** (app vs 25 PDFs de ingeniería IA on-device, 15 únicos, 5 clusters, ~66% conforme) en el fichero de plan: hallazgos C1-C12 (deriva de modelo, regla-7B/router-trap, fallo silencioso de extracción, COMMANDER==EXPERT, mmap-SIGBUS, FTS5-off/BM25, GPU nunca usada n_gpu_layers=0, PII-sandwich a medias, "NPU-First" engañoso). **Modelo unificado (decisión operador)**: `unsloth/gemma-4-E4B-it-qat-GGUF` (UD-Q4_K_XL) en TODOS los tiers, purgada la config muerta Gemma-3-1B-Heretic-Uncensored (commit `cbe5af7`). **Caveats device**: verificar que llama.rn 0.11.0 carga Gemma 4 (si no, subir versión); E4B puede dar OOM <~4GB→fallback UD-Q2_K_XL. **Correcciones inmediatas HECHAS** (commits `cbe5af7`+`81a4509`): C1/C4 (modelo unificado), C3/C9/C10. **2ª PASADA de audit con verificación** (commit `a9949d4`+V-RA3): 3 nuevos safe fixes (N-GC throttle streaming/jank, N1 purga Nomic muerto, S2 borra dir gramáticas muerto) + descartados 4 errores de agente (N2 K/V, S5 maxTokens, V-RA2 cache muerta, V-RA4 recency). **Rama `review/ia-surface` = ~22 commits** listos para smoke test → merge PR#1. **Pendiente = SOLO device/decisión**: cargar modelo Gemma 4 (¿llama.rn 0.11 lo soporta? subir si no), OOM<4GB, embedding-dim E4B vs 256, use_mlock/Jetsam, RoPE, GPU (n_gpu_layers), Flash-Attn, BM25/IDF, pre-filter; y S6 (consolidar intents, dentro de PLAN 2). **3er doc "Resumen app móvil" (02/07, superset 2026)** → 14 ideas nuevas (K1-K14 en el plan). **Aplicado (commit `1511f05`)**: K1 (entitlements iOS increased-memory-limit + extended-virtual-addressing — OBLIGATORIO para cargar E4B en iPhone 8GB o Jetsam aborta) + K2 (use_mmap en vez de use_mlock en LlamaService+LlamaEmbedder). **Necesitan NATIVO (no JS)**: K3 (onTrimMemory fino — el desalojo AppState 30s ya cubre lo crítico), K6 (getThermalHeadroom = fix real de F-04/INF-07). **Estratégico K4**: la app corre CPU-only (n_gpu_layers=0) + llama.rn NO usa NPU → peor modo del doc para batería/térmico. **Verificar en device**: ¿LlamaEmbedder carga 2ª copia de E4B en RAM (~8GB)? ¿unloadModel libera ambos? **K5**: cuantizar KV puede colapsar seguridad silenciosamente (validar rechazos, no solo perplejidad). Rama `review/ia-surface` = 24 commits. **(02/07) PLAN 2 roles EN CURSO**, rama `refactor/roles-catalog` (desde review/ia-surface): **F0+F1 hechos** (commits `9515024`+`19d2652`+`0ac476c`) — RoleCatalog.ts canónico (13 roles: 10 ACTIVE+3 ROADMAP, 19 personas via aliases, traits fusionando 4 fuentes: registry+AGENT_PERSONAS+inlines+TEMPERATURE_CONFIG) + RolePromptBuilder único + 11 agentes migrados + AGENT_PERSONAS borrado (PromptFactory @deprecated hasta F7); tsc 0, suite 91/91. **F2 HECHO (commit `aaee30f`)**: RoleGuardrails.ts (CONVERSATIONAL `<speak>` vs STRUCTURAL JSON, common language/privacy/anti-injection); PersonaContext.mode; PersonaFactory.create(role,ctx,mode) quita cláusula anti-JSON+stop_words en STRUCTURED; AIEngine.generate elige guardrails por modo y no aplica GOLDFISH-LOW a structured; 10 agentes structured pasan mode:'STRUCTURED'; arreglada contradicción JSON-vs-`<speak>`. tsc 0 · roleCatalog 12/12 · roleGuardrails 6/6 (rápido, sin deps nativas) · aiRouting 10/10. **F3 HECHO (commit `e55e66a`)**: routing reubicado a `roles/RoleRouting.ts` (fuente única, `L0_Manager` delega); cascada reproducida VERBATIM (behavior-preserving) salvo **drift fix training_plan/session_*/session_management → TRAINER** (era EL_PROFE, un mentor; TrainerAgent sirve EVENT_TRAINING igual → riesgo nulo). Test roleRouting 29/29 (tabla intent→persona/dominio + invariante anti-drift `resolveDomain(intent)∈eventos del rol`). **Hallazgo documentado**: `academy_scouting` cae en `includes('scouting')`→persona SCOUTER ANTES de la regla CANTERA (sombreada, muerta). **Deferido a F5+device** (cambia prompts en runtime): canonicalizar navigate/ops como routing interno, unificar effectiveRole, generar suscripciones AgentRegistry desde catálogo, cablear/podar 5 agentes muertos. **F4 HECHO (commit `4dc4e75`)**: `roles/RoleSchemas.ts` = contrato de salida zod por rol STRUCTURED (9: Tactician/Analyst/Fisio/Psicologo/Scouter/Trainer/SetPiece/Cantera/Press, fieles a la shape real de cada agente, `.passthrough()`) + `validateRoleOutput(roleId,data)` no-destructivo (nunca lanza; en fallo devuelve data original + issues). Test roleSchemas 14/14. **Deferido a device** (cambia lo que emite el modelo): cablear enforcement (rechazar+re-preguntar), few-shot en MID, simplificación real de schema en LOW. **F5 HECHO (commit `b235b3f`)**: (a) `execution_plan` — el prompt MANAGER exigía JSON con execution_plan (salida MUERTA, solo la lee el TacticalOrchestrator muerto + grammar ACTION_PLAN) y contradecía su uso conversacional (ManagerAgent/fallbacks/summarization); el router ya es interno (ROUTER_ENUM_GRAMMAR) → prompt MANAGER reescrito a conversacional (alineado con COACH). (b) Veto Fisio — `GuardianAgent.validateAction` (ACWR fail-safe GS-03) **hallazgo honesto: solo lo invoca el TacticalOrchestrator MUERTO** → salvaguarda médica sin cablear a flujo vivo; F5 lo declara en catálogo (`FISIO.medicalVeto=true`) + test de lógica (danger→block/warning→warn/safe/unknown→flag/non-physical). Test guardianVeto 8/8. **Deferido a device**: cablear el veto al dispatch vivo (voiceToAction/ActionDispatcher) — empieza a bloquear acciones. **F7 HECHO (commit `5808237`)**: borrado de código PROBADO muerto (tsc 0 + suite roles 84/84 verde después) — `TacticalOrchestrator`+`SemanticCache` (clúster mutuo muerto), agentes `AcademyAgent`/`PhilosophyAgent`/`PizarristaAgent`/`SetPieceAgent` (nunca suscritos, solo los referenciaba el orquestador muerto), `PromptFactory` (sin uso desde F1), grammar `GRAMMARS.ACTION_PLAN` (último resto de execution_plan). AgentRef recortado; MENTOR.agent→null. **HALLAZGO/corrección**: `NewspaperEditorAgent` NO estaba muerto (lo usa `NewspaperService`/pantalla periódico, solo que no suscrito al bus) → NO borrado, nota del catálogo corregida. **Deferido (churn de tipos amplio)**: quitar `voiceQuality` + dedup de duplicados AI_PERSONA/registry (ESTRATEGA/PIZARRISTA/CEO…) — tocan el enum + muchos consumidores → device-verified. **F6 HECHO (commit `9651ef6`)**: `roles/LearningLoop.ts` — (1) `verifyStructuredOutput(roleId,data)` = validador runtime de salida STRUCTURED contra el contrato F4 (no-destructivo, avisa en violación); (2) `recordCorrection(input,roleId)` = **revive el write path muerto** (`AgentCorrectionStore.saveCorrection` nunca se llamaba → el router LEÍA few-shot que nadie ESCRIBÍA; ahora catalog-aware rol→persona, trim, ignora vacío/rol desconocido). Test learningLoop 7/7 (gotcha: mock con `jest.fn()` DENTRO de la factory + recuperar tras import, si no TDZ del hoisting). **PLAN 2 = F0-F7 TODAS cerradas y verdes** (8 commits en `refactor/roles-catalog`: `9515024`→`9651ef6`; rama NO pusheada). **Deferido a DEVICE/UI** (marcado en cada fase): canonicalización total de routing navigate/ops + effectiveRole (F3), enforcement de schemas + few-shot MID + simplificación LOW (F4), cablear veto al dispatch vivo (F5), quitar voiceQuality + dedup enum AI_PERSONA duplicados (F7), y el cableado UI del learning loop + ShadowObserver (F6). **Verificado F2-F6**: tsc 0 · roleCatalog 12/12 · roleGuardrails 6/6 · roleRouting 29/29 · roleSchemas 14/14 · guardianVeto 8/8 · learningLoop 7/7 · aiRouting 10/10 (91 tests). Junto a `review/ia-surface` y PR#1, `refactor/roles-catalog` espera el mismo smoke test en dispositivo. Gotcha jest: SIEMPRE `--runInBand --forceExit` (timers de ThermalMonitor impiden que jest salga → node zombis apilados que ahogan CPU; ANTES de cada tsc/jest `taskkill //F //IM node.exe`; 1er run frío ~150s, warm ~25s; suite completa serie >7min = usar subsets). **(02/07) AUDITORÍA CONOCIMIENTO "roles" (28 PDFs→13 únicos + total 26.8k palabras, LEÍDO AL 100%)**: no es ingeniería IA — es metodología fútbol (Seirul·lo/Frade), UX de campo, modelo de roles del club, gobernanza. Plan profundo aprobado en `~/.claude/plans/c-users-pau-iamasters-os-projects-mente-abstract-acorn.md` (dimensiones A-J, prioridad P0/P1/P1b/P2). **Decisiones operador**: club multiusuario · arranque P0. **Validación estratégica**: la arquitectura on-device (Gemma 4 E4B) ES el "Edge AI/SLM-First" que prescribe Mundial 2026. **P0 HECHO en rama `feat/methodology-rigor`** (desde refactor/roles-catalog): **A1 (commit `3236966`)** `loadMath.ts` puro = EWMA-ACWR + RAG individualizado por z-score (cap físico ≥1.5 siempre; mensaje de intervención no solo color) cableado en `loadCalculation.calculatePlayerLoads`; test 18/18. **B1 (commit `addca76`)** `spatialAlerts.ts` puro = viz densa→alertas de texto cualitativas en modo campo (bloque estirado/junto/adelantado desde los points del heatmap) + prop `field` en MatchHeatmap; test 12/12. tsc 0. **P1 EN CURSO (mismo rama `feat/methodology-rigor`)**: **C1+C2 (commit `31bce66`)** `methodology.ts` puro = taxonomía (6+2 estructuras Seirul·lo, 4 niveles Roca, momentos AO/DO/RD/CT, CLS 1-5, matriz Roca) + morfociclo con intención por día (MD+1…MD-2, días alta tensión MD-4/-3) + `validateTaskForDay` (avisa alta tensión/CLS fuera de día); test 12/12. **D1 (commit `26653a5`)** ANALYST debe emitir `consigna` accionable, no solo métrica (dato→consigna, pilar analytics-practice gap): campo `consigna` añadido al schema F4 + prompt ANALYST; test roleSchemas+learningLoop 22/22. **C3 (commit `add5b64`)** `gameModel.ts` puro = Modelo de Juego estructurado (principios/subprincipios/sub-subprincipios por momento + código + perfiles TTPM por posición) + `validateGameModel` (cubre 4 momentos) + `fitTTPM` (0-1, base del FitScore de scouting F1); test 8/8. **+4 módulos puros más**: **A2 (`da9e019`)** `wellness.ts` = sRPE Foster + monotonía/strain + cuestionario wellness (score/flags) + `blendReadiness` (subjetivo+objetivo, avisa discrepancia=sesgo RPE); 11/11. **F1 (`0f526bd`)** `scouting.ts` = normalización de posesión + `unicornReport` (>N SD, mantiene outliers visibles = anti-exclusión algorítmica) + percentiles + KNN sustitutos + reexporta `fitTTPM`; 11/11. **B2/B3 (`9d93940`)** `tacticalMetrics.ts` = Line Bypass Score + arquetipo de pase + umbral red pases (≥13) + xG acumulado + `controllingTeam` control de espacio con velocidad (por qué gana a Voronoi estático); 11/11. **A3 (`bbd59e6`)** `psycholight.ts` = semáforo RTP psicológico (dolor/ajuste/miedo/adherencia, enfoque por tarea, no comparación pre-lesión); 8/8. **CAPA PURA DEL PLAN COMPLETA — 13 commits en `feat/methodology-rigor`** (`3236966`→`c8f48b8`), **13 módulos PUROS + schema D1 + catálogo D3 + cableado A2**, tsc 0, **154 tests nuevos verdes juntos** (15 suites, sin deps nativas → rápidos). Módulos: `loadMath`(A1 EWMA-ACWR individualizado), `spatialAlerts`(B1 viz→texto), `methodology`(C1/C2 taxonomía+morfociclo), `gameModel`(C3 Modelo de Juego+fitTTPM), `wellness`(A2 sRPE/Foster/readiness), `scouting`(F1 unicornio/KNN/percentiles/normaliz-posesión), `tacticalMetrics`(B2/B3 LBS/arquetipos/pitch-control-velocidad), `psycholight`(A3 semáforo RTP), `rbac`(H1 permisos club, médico 2-tier, no-export-médico), `adoption`(G6/G3 TDTP/FOS/DAFC/IRAP+90/10+quick-win), `messaging`(G5 canales segmentados+guardas médico/menores), `scoutingFunnel`(F2 Kanban Big Board), `sspChecklist`(C4 gate de tareas); + D1 ANALYST→consigna (schema F4); + D3 desdobla DIRECTOR_DEPORTIVO de COACH (CEO); + cableado A2 monotonía/strain en loadCalculation. **TODO lo pendiente del plan es CABLEADO device/UI** (la lógica pura ya está construida y testeada): enganchar cada módulo al wizard/stores/pantallas/runtime de agentes, Wellness Log del jugador, RTP flow, RBAC al backend Supabase RLS, roles nuevos al routing, feedback clips/pizarra dinámica (D2). Va con el smoke test en dispositivo. NO pusheada. **(03-04/07 sesión cableado UI) 10 módulos CABLEADOS a UI en `feat/methodology-rigor`** (OJO repo real = `projects/mt/mobile`; `projects/mt` tiene un `.git` accidental en `master` a limpiar): `3dcb8aa` Wellness A2 (pantalla `wellness-log`+widget readiness real en player-detail sustituyendo el `matchFit` fabricado y barras mock), `bb43d1b` RTP A3 (`rtp-assessment`+semáforo en tarjeta de lesión+gate al ALTA en rojo), `6e9f98d` RBAC H1 (roleStore+usePermissions+`role-settings` en Más›Rol activo i18n+gate MEDICAL_DETAIL en lente biométrico), `6cfeca7` morfociclo C2 (`microcycleDayLabel`/`auditDayLoad`+panel intención en MicrocycleDayDetail), `97aa0ab` gameModel C3 (`game-model` editor identidad+principios por momento+cobertura), `ee3bd1a` scouting F1 (percentil en plantilla+badge perfil atípico en player-detail, datos evaluation_* reales), `10cb536` sspChecklist C4 (SspChecklistCard en exercise-create tácticos), `515ebbc` scoutingFunnel F2 (Big Board Kanban+prospectStore), `d268300` messaging G5 (`message-composer` router+guardas legales), `eecc9bc` adoption G3 (quick-win tras registrar wellness). Patrón: store local-first AsyncStorage+reducers puros testables, sync Supabase diferido. tsc 0 por commit; **112 tests verdes juntos** (14 suites). **NO pusheada** (mismo gate smoke test). **DEFERIDO por honestidad (sin superficie de datos real en esta app)**: tacticalMetrics B2/B3 + spatialAlerts B1 (necesitan tracking/pass data = dominio proyecto FVI; MatchHeatmap ya cablea B1 pero sin llamador ni datos), adoption metrics G6 (TDTP/FOS/DAFC/IRAP = telemetría org no registrada), y **RLS backend de RBAC en Supabase prod** (pendiente confirmación de Olaf antes de tocar prod). **(06/07) CONSOLIDACIÓN DE LAS 4 RAMAS (en seco)**: hallazgo = el stack se había bifurcado en `1511f051` → los 2 commits que hicieron ARRANCAR el device (`6a088de` plugins que `app.json` exige `withAndroidTTS`/`withAndroidAttributes` + `be254e3` ModelLoadingBanner+compuerta anti-crash+news fix) vivían SOLO en `review/ia-surface` y NO en la línea de merge → mergear la cima habría roto el build. Re-linearizado (rebase, cero solape de ficheros → cero conflictos): `roles-catalog` y `methodology-rigor` rebasadas sobre la punta de `review/ia-surface`; stack ahora **perfectamente lineal apilado, todo +N/-0** (main←remediacion+29←review+26←roles+9←methodology+25). **Backups**: `backup/roles-catalog-pre-relinear`(9651ef6), `backup/methodology-pre-relinear`(eecc9bc). **Hashes nuevos**: roles-catalog `3dcc51e`, methodology-rigor `d427686`. **Verificado post-rebase**: methodology tsc 0 · jest 37 suites/312; review/ia-surface tsc 0 · jest 13/74. Decisión operador = **PRs apilados revisables**. **PLAN DE PUSH+MERGE** (pushear ≠ mergear; el gate smoke-test aplica solo al MERGE a main, no al push): **Paso 0 — HECHO (06/07)**: pusheadas las 3 ramas a origin (primer push limpio, sin `--force`) + abiertos los 3 **PRs draft apilados**: **PR#2** review→remediacion, **PR#3** roles→review, **PR#4** methodology→roles (todos DRAFT = señalan gate smoke-test; PR#1 remediacion→main sigue OPEN). Repo `Capitalmaxequity/LLM`. **Paso 1 — HECHO (06/07, gate device ANULADO por decisión explícita del operador con el riesgo sobre la mesa)**: las **4 ramas mergeadas a main** (`origin/main` @ `ab974c7`, 4 merge commits: #1 remediacion, #5 review [reemplazó al #2, que GitHub cerró al usar `--delete-branch` sobre una base con PR hijo], #3 roles, #4 methodology). Cada PR retargeteado a main y mergeado en orden (`--merge`, sin `--admin` — `UNSTABLE`/lint roto no era check obligatorio; sin `--delete-branch` tras el incidente del #2). Árbol de main IDÉNTICO a la cima verificada `d427686` → **tsc 0 aplica a main**. Device fixes (ModelLoadingBanner + plugins app.json) confirmados en main. **⚠️ EL SMOKE TEST EN DEVICE SIGUE SIN CORRERSE** — main está verde en compilación/lógica pura pero NO validado en móvil real (¿Gemma 4 carga en llama.rn 0.11?). Si peta en device, el fix irá en rama nueva sobre main. Ramas locales+remotas y backups (`backup/roles-catalog-pre-relinear`, `backup/methodology-pre-relinear`) sin borrar. **Runbook del smoke test listo**: `mobile/docs/SMOKE-TEST-BLOQUE0-RUNBOOK.md` (mecánico, copy-paste: pre-flight adb, `expo prebuild`+`run:android` por los plugins app.json, logcat filtrado, árbol de decisión keyed a tags reales `LlamaService.initLlama`/`ModelAssetManager` → PASA / arch (subir llama.rn>0.11) / OOM (fallback UD-Q2_K_XL) / matada-en-descarga; + plantilla de reporte). En rama `docs/smoke-bloque0-runbook` → **PR #6** (docs-only). Criterios detallados en el hermano `SMOKE-TEST-modelo-gemma4-y-audit.md` (Bloques 0-3).
- **Informe de Jugador con IA** (`projects/informe-jugador-ia/`): embudo CERRADO por hoy (02-03/07) — 8 commits en `feat/fase-1` (`c659ba7`→`3ca2992`), **todos PUSHEADOS** (verificado `git log origin/feat/fase-1..HEAD` vacío). Motor (share-card+corpus reels separado) + 2 canales (Telegram manual, ManyChat automático vía callback) + gate honesto (`no_onball_signal`, no entrega tarjetas sin señal medida) + guía de vídeo v2 con diagrama+3 ejemplos + marca del servicio creada (`brand-context/brand-system.md`+`voice-profile.md` v0) + 3 fixes WCAG. Detalle completo en `synapsis/daily-summaries/2026-07-03.md` (Sesión 2) y `context/decisions-log.md`. **PENDIENTE (Olaf)**: cuenta ManyChat Pro+IG Business+WhatsApp + túnel cloudflared + elegir 2 pilotos. Gate negocio: 3 ventas en 4 semanas. **(03/07) AUDITORÍA DE EMBUDO end-to-end** → `auditoria-embudo.md` (checklist priorizado 🔴🟠🟡). **Arreglado hoy** (sin commitear): L1 (muestra gratis enseñaba a grabar el vídeo que NO sirve para Scouting — sembrado el spec de pago en el puente §2b del embudo), L2 (landing mostraba solo guía de móvil → añadido desdoble Express-móvil vs Scouting-cámara-fija+líneas, verificado en preview :8200), L5 (blindado `tier:auto` en manychat-setup, verificado `app.py:414` auto→Tier1), O1 (prefill WhatsApp en los 6 CTA de la landing). **Decisiones de Olaf TOMADAS Y APLICADAS (03/07, sin commitear)**: L3/O4 → Meta Pixel + form de respaldo (n8n→Supabase) en la landing; L4 → compuerta de edad en ManyChat (doc); O3 → pago solo tarjeta (Stripe Payment Links); **O2 → seis-sombreros (Alt 5+Alt 3): PRECIO POR COBERTURA, un solo producto** — se retira "Express 79€/Scouting 249€", ahora "informe según tu vídeo": móvil/Tier C **99€** (lectura parcial) · cámara fija partido completo/Tier A/B **249€** (club). Muestra gratis primero como gancho. Plan B: si 99€ no vende en 4 sem, eliminar tier bajo. Aplicado en landing (sección "Un informe, según tu vídeo" reescrita, verificada en preview :8200) + brief + plantilla + spec-intake + casos-piloto + embudo + README. Análisis en `projects/seis-sombreros/2026-07-03-express-79-pricing.md`; decisión en `context/decisions-log.md`. **Olaf debe rellenar en la landing**: `{{TEL_WHATSAPP}}`, `{{META_PIXEL_ID}}`, `{{FORM_WEBHOOK}}`; + crear cuenta Stripe (2 payment links 99/249) + flujo n8n del form + compuerta de edad en ManyChat. **Vacío a medir**: tiempo-persona real de entregar un Tier C (si >~30min, revisar suelo 99€). **(03/07) REPO DEDICADO creado**: `Capitalmaxequity/informe-jugador-ia` (privado, GitHub), repo git anidado en `projects/informe-jugador-ia/` (el padre ignora `projects/briefs/*`), primer commit `88fb780` pusheado (17 archivos: landing + docs + análisis precio). **(03/07) SETUP TÉCNICO — plan en `setup-tecnico.md`** (Fase 0 cobrar+deploy / F1 captura / F2 ManyChat / F3 entrega). **Fase 1 construible HECHA Y VERIFICADA**: proyecto Supabase nuevo **`informe-jugador-ia` = `twbessnbkkmtymvvztms`** (org `syrewkypekrzdhvokpaf` halfproductionspain, eu-north-1, free), tabla `public.leads` (RLS on, sin políticas públicas), **Edge Function `lead-intake`** (POST público verify_jwt off, CORS, validación, honeypot, insert con service role) endpoint `https://twbessnbkkmtymvvztms.supabase.co/functions/v1/lead-intake` — ya cableado en el `data-endpoint` del form de la landing + honeypot `website`. Probado end-to-end (válido→200 inserta, honeypot→200 sin insertar, inválido→422; fila de prueba borrada, tabla a 0). **Decisión arquitectura**: form usa Edge Function, NO n8n (n8n MCP no conectaba; se reserva para Fase 3 entrega ManyChat). `nivel` del form → columna `categoria`. **Pendiente Olaf**: Stripe (2 payment links 99/249), Meta Business→pixel `{{META_PIXEL_ID}}`, deploy landing (Netlify/Vercel), rellenar `{{TEL_WHATSAPP}}`, ManyChat Pro+IG+WhatsApp+cloudflared+flujo (Fase 2). Nota Supabase: existen además proyectos `mente-tactica` (activo) y `futbol`/default (INACTIVE, sin usar) en la misma org. **(04/07) MEJORA INTEGRAL DE LA LANDING (7 pases, plan `~/.claude/plans/quiero-hacer-una-mejora-floofy-lightning.md`, todo verificado en preview, SIN commitear)**: (1) **Legal/RGPD** — banner de cookies que BLOQUEA el pixel hasta aceptar (privacy-first, verificado: sin consentimiento no carga fbevents.js), `legal.html` nuevo (aviso legal+privacidad+cookies, placeholders `{{RESPONSABLE}}/{{NIF}}/{{EMAIL_CONTACTO}}`), checkbox de consentimiento obligatorio en el form; (2) **CRO/psicología** — muestra del entregable (share-card SVG inline `#informe-ejemplo`, sellado "ejemplo ilustrativo") en hero (2 col) + Casos vacíos reconvertidos a "todavía no hay casos, y preferimos decírtelo" + "sé de los primeros" con escasez honesta (1 mañana/sem) + chip de garantía + 2 FAQ (privacidad, tiempo); (3) **copy** en registro B, sin AI-tells; (4) **diseño** — semáforo con hex EXACTOS de marca (#16A34A medido/#D97706 estimado); (5) **SEO/GEO** — JSON-LD Service+2Offer+FAQPage+Organization, canonical/og/twitter, `{{SITE_URL}}` nuevo placeholder; (6) **rendimiento** — Tailwind compilado (16.4KB) e INLINEADO, CDN eliminado (rebuild vía `landing/tailwind.config.js`+`src.css`); (7) gate usabilidad OK. **Pendiente del deploy**: exportar `assets/og-cover.svg`→`og-cover.png` 1200×630 (WhatsApp/FB no renderizan SVG), WebP de los PNG (no había cwebp local), acotar CORS de la Edge Function al dominio. **Screenshots del preview caídos toda la sesión** (renderer; verificado por DOM/eval). Nuevos ficheros en el repo `informe-jugador-ia`: `legal.html`, `assets/og-cover.svg`, `tailwind.config.js`, `src.css`, `setup-tecnico.md`, `README.md`. **(05/07) SESIÓN MARKETING + MARCA v2**: Cadenas 1+2 de skills marketing ejecutadas (8 instaladas) → 8 docs en `marketing/` (product-marketing, lista-prospeccion 5-3-1, oferta-v2, psicologia-aplicada, calendario, pieza LinkedIn, radar config). **Radar viral EN VIVO** (Apify, `projects/marketing-viral-radar/2026-07-05/radar.md`): H1 "3 cosas que mira un ojeador" confirmada con datos, ola "pruebas de verano". **Arranque IG completo** (perfil-kit/estrategia/piezas-arranque/calentar-cuenta 5-3-1/prompt-buscar-cuentas). LinkedIn kit hecho pero APLAZADO. **REDISEÑO MARCA v2**: rechazado v1 (dark+verde) → investigación tendencias 2026 (agente, 10+ fuentes: verde-sage fatigado, dark-SaaS no diferencia, badge=dashboard) → elegida **AB-1 "Expediente de ojeador"**: papel `#F5F1EA` + grafito `#1C1917` + terracota `#C2410C` + **semáforo como SELLO** ("no cubierto" discontinuo) + 3 voces tipográficas (serif titular/Inter cuerpo/mono metadatos). Formalizada en `brand-context/brand-system.md` v2 (v1 en anexo, MODIFICADO en repo padre sin commitear). **Pieza 1 regenerada en v2**: 7 slides `marketing/assets/piece1-v2-01..07.svg`. **PENDIENTE**: (1) exportar SVG→PNG con **Fraunces** embebida + resto del grid IG; (2) **CRM Supabase** esquema `crm` SIGUE bloqueado (MCP solo-lectura → reiniciar Claude Code; SQL en plan `~/.claude/plans/gentle-swimming-eich.md`); (3) re-skin landing+share-card v1→v2; (4) Olaf: crear cuenta IG (handle `@informejugador.ia`/email/foto) + dar red cálida real para forecast/CRM. Repo anidado: `README.md` M + `marketing/` sin commitear.
- **Cliente Jesús Roiget — Director v2 n8n**: workflow `nlow3ijQLqj9ho7t` activo. Siguiente: probar agenda por voz → FAQ RAG o deploy PC Jesús.
- **Agencia FIFA**: pendiente licencia + `/add-client agencia-fifa`.

## Notas de entorno
- **(05/07) 8 skills nuevas instaladas para FVI** (frentes A/B/C/E): `pytorch-lightning`, `shap`, `statistical-analysis`, `impeccable`+`design-taste-frontend`, `marketing-pricing`+`marketing-launch`+`marketing-sales-enablement`. **Reinicio de Claude Code hecho (05/07)** — ya cargadas y activas. ~54 skills activas (sobre tope blando 50; quitar si molesta).
- **(05/07) Auditoría de integraciones/MCP cerrada**: Firecrawl (key probada, scrape real OK), NotebookLM (login Google hecho, sesión persistida en `chrome_profile`), Gmail+Calendar (confirmados operativos: `search_threads`/`list_events` responden), HeyGen (conectado vía OAuth remoto, cuenta free confirmada con `get_current_user`), n8n (API real accesible + MCP `n8n-mcp` conectado tras reinicio, workflows visibles). **Pendiente**: WhatsApp/Evolution API en el VPS Tailscale (100.127.241.1) — contenedor revivido (estaba caído 2 semanas por falta de Redis) pero sigue sin Redis bien configurado (loop de error no bloqueante) y sin número vinculado (falta que Olaf escanee QR). daily-brief funciona pero solo corre con la app abierta — Olaf decidió montarlo él mismo en **Claude Cowork** para que corra sin depender del PC.
- Dev en Windows (Python 3.14 problemático para ML → Docker py3.11 para FVI). Copytrader tests: `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1`.
- **Python arreglado (04/07)**: `python`/`python3` ya son el real 3.14.2 (desactivados los alias de Windows Store, stub falso exit 49). Rompía el observer de Sinapsis (obs. paradas 30/06); parcheado+verificado. Scripts Python: validar `--version`, no `command -v`. Skills datos/ML (Polymarket) ya sin truco. Memoria `windows-python-shim`.
- **Skills OS (04/07)**: proyecto `nuevas-skills` CERRADO + **Lote 2 añadido misma rama** (mismo día, sesión aparte): 20 skills nuevas (vídeo/voz/avatar/scraping-con-runner/legal/ventas/finanzas/CRM/radar-viral), 6 fusionadas en skills existentes (no duplicadas), triado contra ~24 fuentes externas (Claura + trending). Catálogo **107→127** (self-healing intacto), activas **46→47** (solo `tool-scrape-router` instalada). Auditoría de integridad + activación tras el crecimiento → **sistema sano** (2 falsas alarmas aclaradas en CLAUDE.md: duplicación Core+Biblioteca es por diseño, `investigacion-mercado` es skill global). Router real = tablas CLAUDE.md + fallback `find-skills`→`npx skills` (verificado funcionando en Windows). **9 commits en `feat/nuevas-skills`, SIN pushear** (esperando decisión + coordinar con PR #8 upstream, aún sin mergear).
- NUNCA `git add -A` en `projects/app video` (colaría empresa-scouting).
- VPS Contabo `dev@144.91.91.233`: **(30/06) Claude tiene SSH por clave dedicada** → `ssh polymaster-vps` (no interactivo). El `git pull` del VPS NO funciona (remote HTTPS sin credencial) → código por `scp`+`docker compose cp`. BD copytrader en volumen Docker `sqlite_data:/app/data/bot.sqlite`.
- `docker-compose.yml` del copytrader: bloque `environment:` PISA el `.env` (por eso existe `docker-compose.live.yml`).
- Glob/Grep amplios cuelgan por `.venv`. Acotar el path.
- n8n vía cloudflared efímero (si reinicia cambia URL). Prod pendiente: dominio + Caddy TLS.

## Decisiones pendientes
- **Polymaster — URGENTE**: dashboard VPS `http://144.91.91.233:8010/` EXPUESTO sin auth. Acción Olaf: `sudo ufw deny 8010/tcp` + túnel SSH + go-live prep.
- **Mente Táctica**: rotar PAT Supabase `sbp_4849…` (Account→Tokens, revocar el viejo).
- FVI Frente A: entrenar los 4 modelos propios cuando Olaf alquile GPU (Vía B eventos = la que sube shot/recovery). Slots montados; guía `docs/GUIA_MODELOS_PROPIOS_GPU.md`.
- Commitear `.claude/settings.json` (Fix C: ingest SessionStart `async`) — esperando OK.
- **Skills — pushear Lote 2**: ¿pushear los 9 commits de `feat/nuevas-skills` ahora, o esperar a que mergeen PR #8 primero? Decidir próxima sesión.
- **`trive v2`**: operador iba a pasar el link/nombre correcto de la 5ª fuente pedida en el lote de skills — sigue pendiente.
- **WhatsApp (Evolution API, VPS Tailscale)**: pendiente que Olaf escanee QR para revincular número; fix de fondo de Redis pendiente (tarea disponible, no lanzada).
- **daily-brief en Cowork**: Olaf va a montar la rutina matutina (Gmail+Calendar, 08:30 Europe/Madrid) directamente en Cowork para que corra sin depender del PC — no lo he hecho yo.

---
*Actualizado: 2026-07-06 (Mente Táctica: consolidación de las 4 ramas en seco — re-linearizadas a stack apilado limpio, device fixes recuperados en la línea de merge, tsc+jest verdes, backups guardados; gate sigue = smoke test device.)*
